{
  "scripts": {
    "install-packages": "(cd front-end && npm install) && (cd back-end && npm install)",
    "client": "cd front-end && npm run build",
    "server": "cd back-end && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "(npm run install-packages) && (npm run client) && (npm run server)"
  },
  "dependencies": {
    "concurrently": "^6.0.2"
  }
}
